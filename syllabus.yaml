# Setting up Environment for TypeScript
1. Download any browser (chrome, brave, edge etc...)
2. Download nodeJS
3. Download any editor (vscode, neovim, notepad++ etc....)


# Installing TypeScript
1. npm install typescript -g (NOTE: if you are using mac or linux add sudo before this command)
(INFO: This command will gonna install typescript and typescript compiler)


# Installing Essential Vs Code Extensions
1. ESLINT
2. JavaScript and TypeScript Nightly
3. Live Server
4. Prettier - Code Formatter
5. Pretty TypeScript Errors


# Setting up & compiling Typescript project
1. TypeScript Setup
2. Setting up a TypeScript project
3. Configuring tsconfig.json
4. Compiling TypeScript
(INFO: First create a new file app.ts then run this command in terminal: (tsc app.ts) that will create a new file app.js so now when you write the code in .ts file and then the typescript will compile the code into that file .js file. Now configure tsconfig.json file just run this command in terminal: (tsc --init). If you don't want to run this command again & again to compile the TypeScript code (tsc app.ts) then use this commadn instead (tsc --watch) that will compile the code automatically when you make the change in .ts file)


# Introduction to Basic Types
